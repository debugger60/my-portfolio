# ============================================================
#  FRONTEND — ENVIRONMENT CONFIGURATION
# ============================================================
#  Copy this file to `.env.local` (frontend/ folder).

# URL of the Express backend (see backend/ folder).
#   • Local dev:  http://localhost:4000
#   • Production: https://your-backend.onrender.com (or wherever it's deployed)
NEXT_PUBLIC_API_URL=http://localhost:4000
